# Archive

Empty as of this organization pass (2026-09-10).

Every screen exported in `stitch_responsive_dark_budget_dashboard.zip` mapped to exactly one expected page with no duplicate or superseded versions found, so nothing needed archiving.

Use this folder for older or uncertain design-reference versions once a page's design is regenerated in a future Stitch pass — keep the superseded file here rather than deleting it, per `docs/design-reference/README.md`'s source-of-truth rules.
